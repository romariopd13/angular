import { FiltroArrayPipePipe } from './filtro-array-pipe.pipe';

describe('FiltroArrayPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArrayPipePipe();
    expect(pipe).toBeTruthy();
  });
});
